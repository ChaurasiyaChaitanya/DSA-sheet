class Solution {
    public boolean canJump(int[] nums) {
        int n = nums.length;
        int finalIdx = n - 1;

        for(int i=n-2; i>=0; i--) {
            if(i + nums[i] >= finalIdx) {
                finalIdx = i;
            }
        }

        return finalIdx == 0;
    }
}