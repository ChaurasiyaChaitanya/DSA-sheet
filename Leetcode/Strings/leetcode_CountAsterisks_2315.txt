class Solution {
    public int countAsterisks(String s) {
        int result = 0;
        boolean pair = false;

        for(char c : s.toCharArray()) {
            if(c == '*' && !pair) {
                result++;
            } else if(c == '|') {
                pair = !pair;
            }
        }

        return result;
    }
}