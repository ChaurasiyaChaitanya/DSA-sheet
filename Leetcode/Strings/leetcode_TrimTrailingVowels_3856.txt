class Solution {
    public String trimTrailingVowels(String s) {
        int n = s.length();

        for(int i=n-1; i>=0; i--) {
            if("aeiou".indexOf(s.charAt(i)) == -1) {
                return s.substring(0, i+1);
            }
        }

        return "";
    }
}