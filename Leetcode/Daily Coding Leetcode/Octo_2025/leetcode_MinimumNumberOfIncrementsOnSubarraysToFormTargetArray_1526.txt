class Solution {
    public int minNumberOperations(int[] target) {
        int prev = 0, result = 0;

        for(int i=0; i<target.length; i++) {
            int curr = target[i];
            if(curr > prev) {
                result += (curr - prev);
            }
            prev = curr;
        }

        return result;
    }
}