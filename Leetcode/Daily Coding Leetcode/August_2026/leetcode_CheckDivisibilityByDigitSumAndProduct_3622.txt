class Solution {
    public boolean checkDivisibility(int n) {
        int sum = 0, prod = 1, num = n;

        while(num > 0) {
            int digit = num % 10;
            sum += digit;
            prod *= digit;
            num /= 10;
        }

        return n % (sum + prod) == 0;
    }
}