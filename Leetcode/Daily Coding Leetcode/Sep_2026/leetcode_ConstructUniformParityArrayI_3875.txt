class Solution {
    public boolean uniformArray(int[] nums1) {
        // Even - Even = Even
        // Odd - Odd = Even
        // Even - Odd = Odd
        // Odd - Even = Odd

        // Always possible
        return true;
    }
}