class Solution {
    public int mirrorDistance(int n) {
        int num = n;
        int reverseNum = 0;

        while(num > 0) {
            reverseNum = reverseNum * 10 + num % 10;;
            num /= 10;
        }

        return Math.abs(n - reverseNum);
    }
}