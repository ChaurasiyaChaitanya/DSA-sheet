class Solution {
    public int digitFrequencyScore(int n) {
        // int[] freq = new int[10];

        // while(n > 0) {
        //     freq[n % 10]++;
        //     n /= 10;
        // }

        int result = 0;

        // for(int i : freq) {
        //     if(freq[i] > 0) {
        //         result += i * freq[i];
        //     }
        // }

        while(n > 0) {
            int d = n % 10;
            result += d;
            n /= 10;
        }

        return result;
    }
}