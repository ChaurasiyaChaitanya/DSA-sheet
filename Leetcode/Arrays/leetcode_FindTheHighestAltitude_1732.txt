class Solution {
    public int largestAltitude(int[] gain) {
        int curr = 0, result = 0;

        for(int g : gain) {
            curr += g;
            result = Math.max(result, curr);
        }

        return result;
    }
}